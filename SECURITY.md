# Karna 安全政策

## 支持范围

只有 GitHub Releases 中明确标为“当前支持”的版本接收安全修复。所有 `0.17.x` 版本均已撤回，不再支持，也不应继续安装或传播。

## 私密报告

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私密提交安全问题。报告应尽量包含：受影响版本、操作系统、复现步骤、预期与实际结果，以及经过脱敏的日志片段。

请勿在公开 Issue、讨论区或截图中提交：

- API Key、OAuth Token、Cookie 或私钥；
- 真实对话、项目文档、Soul、工作流或个人路径；
- 可直接利用但尚未修复的漏洞细节；
- 未脱敏的安装或运行日志。

如果凭据可能已经泄漏，请先在对应服务商处吊销或轮换，不要等待 Karna 团队确认。

## 发行安全基线

Karna 的发行流程以以下条件作为硬门禁：

1. 干净安装不得导入旧 Hermes/Karna 数据或系统中的模型凭据；
2. 未明确配置、授权并通过连接测试前，不允许模型请求；
3. 安装器必须包含版本匹配的离线运行时，不得在首启克隆源码；
4. 包内容必须通过白名单、凭据、绝对路径、测试数据与旧品牌扫描；
5. 公开发行仓只包含产品文档、许可证与 Release 资产；
6. 稳定版必须经过代码签名，缺少证书时构建直接失败。

## 响应原则

我们会先确认报告是否可复现及影响范围，再决定修复、撤回发行、轮换凭据或通知用户。修复公开前不会披露报告者不希望公开的身份信息。
